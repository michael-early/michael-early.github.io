---
phase: 01-workflow-scaffold
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - README.md
autonomous: false
requirements:
  - TRIG-01
  - SAFE-01

must_haves:
  truths:
    - "README explains exactly how to add CLAUDE_CODE_OAUTH_TOKEN to GitHub Secrets"
    - "README explains the branch protection setup steps with exact UI navigation"
    - "User can verify end-to-end by filing a test issue and observing the Actions tab"
  artifacts:
    - path: "README.md"
      provides: "Setup documentation for CLAUDE_CODE_OAUTH_TOKEN secret and branch protection on main"
      contains: "Settings → Branches"
  key_links:
    - from: "README.md"
      to: "GitHub Settings UI"
      via: "Step-by-step instructions for branch protection"
      pattern: "Settings.*Branches"
---

<objective>
Write the README setup documentation and confirm the complete Phase 1 scaffold works end-to-end via a human verification checkpoint.

Purpose: All setup instructions must exist before Phase 2 begins — the README is a Phase 1 deliverable per CONTEXT.md. The checkpoint confirms the workflow fires correctly in the live GitHub environment.
Output: Updated `README.md` with branch protection and secret setup docs; human confirmation that the workflow behaves as expected.
</objective>

<execution_context>
@/Users/mearly/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mearly/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-workflow-scaffold/01-CONTEXT.md
@.planning/phases/01-workflow-scaffold/01-01-SUMMARY.md

<interfaces>
<!-- Branch protection UI steps — from RESEARCH.md verbatim -->
Branch protection setup:
  1. Go to repository Settings → Branches (under "Code and automation")
  2. Click "Add rule" (or "Add branch ruleset" on newer GitHub UI)
  3. Branch name pattern: main
  4. Check "Require a pull request before merging"
  5. Click "Create"
  Note: Personal repos cannot add bypass actors — protection applies to all users including owner.

Secret setup location:
  Repository Settings → Secrets and variables → Actions → New repository secret
  Name: CLAUDE_CODE_OAUTH_TOKEN
  Value: OAuth token from claude.ai → Settings → Connections → GitHub
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README setup documentation</name>
  <files>README.md</files>
  <action>
Read the current `README.md` first. Then update it to include a clear "Setup" section covering two topics:

**Section: Setting Up the Portfolio Agent**

Include these two subsections:

**1. Add CLAUDE_CODE_OAUTH_TOKEN secret**
Steps to add the key in GitHub UI:
1. Go to repository Settings → Secrets and variables → Actions
2. Click "New repository secret"
3. Name: `CLAUDE_CODE_OAUTH_TOKEN`
4. Value: OAuth token from claude.ai → Settings → Connections → GitHub
5. Click "Add secret"

Note: If the secret is not set, the workflow will fail with: `ERROR: CLAUDE_CODE_OAUTH_TOKEN secret is not set`

**2. Enable branch protection on main**
Steps (exact from RESEARCH.md — do not paraphrase):
1. Go to repository Settings → Branches (under "Code and automation")
2. Click "Add rule" (may appear as "Add branch ruleset" on newer GitHub UI)
3. Branch name pattern: `main`
4. Check "Require a pull request before merging"
5. Click "Create"

Note: On personal repos, branch protection applies to all pushes including the owner. The agent workflow always creates feature branches (`agent/issue-{N}`) and never pushes directly to `main`, so branch protection does not affect the agent.

**Section: How It Works**
Brief description of the trigger behavior:
- Filing an issue as the repo owner triggers the agent workflow
- Non-owner issues are silently skipped
- The agent reads the issue body, calls Claude to make HTML/CSS edits, and opens a Pull Request for review
- No change is ever pushed directly to `main`

Keep the README concise and professional. Do not invent features not yet built. Phase 1 is scaffold only — do not describe Claude API calls or PR creation (those are Phase 2 and 3).
  </action>
  <verify>
    <automated>grep -n "CLAUDE_CODE_OAUTH_TOKEN\|Settings.*Branches\|branch protection\|Require a pull request" /Users/mearly/michael-early.github.io/README.md</automated>
  </verify>
  <done>
- README.md contains a setup section with CLAUDE_CODE_OAUTH_TOKEN secret instructions
- README.md contains branch protection setup steps including the exact UI path "Settings → Branches"
- README.md accurately describes the agent trigger behavior without overpromising Phase 2/3 features
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 1 end-to-end in live GitHub</name>
  <action>Human verifies the workflow fires correctly by filing a test issue and inspecting the Actions tab.</action>
  <verify>
    <automated>MISSING — GitHub Actions trigger behavior requires a live authenticated GitHub environment; no local automated verification possible</automated>
  </verify>
  <done>Owner-filed issue produces a completed workflow run showing agent stub output in the Actions log.</done>
  <what-built>
Complete Phase 1 scaffold:
- `.github/workflows/portfolio-agent.yml` — issues.opened trigger, owner guard, bot guard, secret assertion, checkout, agent stub invocation
- `agent/run.py` — Python stub printing issue env vars and exiting 0
- `README.md` — branch protection and CLAUDE_CODE_OAUTH_TOKEN setup documentation

These files have been committed and pushed to the main branch (or are ready to push).
  </what-built>
  <how-to-verify>
Before verifying, complete any remaining setup:
1. If not already done: Add `CLAUDE_CODE_OAUTH_TOKEN` to repo secrets (Settings → Secrets and variables → Actions → New repository secret)
2. If not already done: Enable branch protection (Settings → Branches → Add rule → pattern: main → require PR before merging)

Then verify the Phase 1 success criteria:

**Test 1 — Owner trigger (TRIG-01):**
- File a new issue on your repo from your owner account
- Go to the Actions tab
- Confirm the "Portfolio Agent" workflow run appears and completes successfully
- Click into the run → "Run agent stub" step → confirm it printed ISSUE_NUMBER, ISSUE_TITLE, and ISSUE_BODY

**Test 2 — Non-owner skip (TRIG-01):**
- If you have a second GitHub account, file an issue from that account
- Confirm no workflow run appears in the Actions tab (silent skip)
- (Optional if no second account — skip this test and note it)

**Test 3 — Bot guard (SAFE-01):**
- This is hard to trigger manually without a bot commit. Inspect the "Skip if bot actor" step in any existing run — confirm it ran and did not exit early (because you are not the bot actor).
- The guard will activate automatically when Phase 3 introduces commit-triggered events.

**Test 4 — Branch protection:**
- Attempt `git push origin main` directly from your local machine (with an uncommitted change)
- Confirm GitHub rejects with: "remote: error: GH006: Protected branch update failed"
- (Only if branch protection was already enabled)
  </how-to-verify>
  <resume-signal>
Type "approved" if Tests 1 and 3 pass (workflow runs, bot guard step visible in log).
Type "partial: [describe what passed/failed]" if some tests did not pass.
Type "blocked: [reason]" if you cannot complete testing (e.g., no CLAUDE_CODE_OAUTH_TOKEN yet).
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
1. `.github/workflows/portfolio-agent.yml` exists with all required steps in correct order
2. `agent/run.py` runs and prints issue env vars
3. `README.md` documents branch protection and secret setup
4. Human confirmed the workflow runs end-to-end on an owner-filed issue
</verification>

<success_criteria>
All four Phase 1 roadmap success criteria are TRUE:
1. Filing an issue as repo owner causes workflow to run; non-owner issue does not
2. Workflow exits cleanly when triggered by bot actor (log message visible)
3. Agent script prints issue env vars and exits 0 — end-to-end plumbing verified
4. Pushing to main is blocked by branch protection; workflow only targets feature branches
</success_criteria>

<output>
After completion, create `.planning/phases/01-workflow-scaffold/01-02-SUMMARY.md` documenting:
- README sections added
- Human verification results (which tests passed)
- Any issues encountered during testing
- Phase 1 overall status (complete / partial)
</output>
